{
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "csv-parse": "^5.0.4"
  },
  "scripts": {
    "develop": "eleventy --serve",
    "build": "eleventy",
    "fetch:linux": "curl -L -o src/_data/donors.csv ${DATA_CSV_URL}",
    "fetch:win64": "curl -L -o src/_data/donors.csv %DATA_CSV_URL%"

  }
}
